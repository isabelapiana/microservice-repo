spring:
  datasource:
    password: root
    url: jdbc:postgresql://localhost:5432/microservice-finance
    username: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL81Dialect
        format_sql: true
        jdbc:
          lob:
            non_contextual_creation: true
    show-sql: true           
